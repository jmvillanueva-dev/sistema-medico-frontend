---
import DashboardLayout from "../../../../layouts/DashboardLayout.astro";
import MedicalEvolutionHistory from "../../../../components/MedicalEvolution/MedicalEvolutionHistory";

const { id } = Astro.params;

if (!id) {
  return Astro.redirect("/medical/patients");
}
---

<DashboardLayout title="Historial de Evoluciones">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <MedicalEvolutionHistory
      client:load
      historiaClinicaId={id}
      onViewDetail={(evolutionId) => {
        window.location.href = `/medical/evolutions/${evolutionId}`;
      }}
      onEdit={(evolutionId) => {
        window.location.href = `/medical/evolutions/edit/${evolutionId}`;
      }}
      onCreate={() => {
        window.location.href = `/medical/evolutions/create/${id}`;
      }}
    />
  </div>
</DashboardLayout>
